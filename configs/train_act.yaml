# LeRobot ACT training config for the SO-ARM100 pick & place dataset.
#
# Run with:
#   .venv/bin/lerobot-train --config_path=configs/train_act.yaml
#
# For a quick smoke test (few steps, CPU):
#   .venv/bin/lerobot-train --config_path=configs/train_act.yaml --steps=20 --policy.device=cpu
dataset:
  repo_id: local/so_arm100_pick
  root: data/local/so_arm100_pick
  # Hold out a fraction of episodes for offline eval loss.
  eval_split: 0.0

policy:
  type: act
  push_to_hub: false
  # Use "mps" on Apple Silicon for faster training; "cpu" always works.
  device: mps
  n_obs_steps: 1
  chunk_size: 100
  n_action_steps: 100
  vision_backbone: resnet18
  pretrained_backbone_weights: ResNet18_Weights.IMAGENET1K_V1
  dim_model: 256
  n_heads: 4
  dim_feedforward: 1600
  n_encoder_layers: 4
  n_decoder_layers: 1
  use_vae: true
  latent_dim: 32
  n_vae_encoder_layers: 4
  dropout: 0.1
  kl_weight: 10.0
# ACT recipe: 50 demos, 2000 epochs, batch 8 (paper reports ~90% on Transfer Cube).
# steps = 2000 epochs * ceil(total_frames / batch_size). Value below is computed
# from the collected 50-episode dataset (see collect50); update it if the
# episode length changes.
optimizer_lr: 1e-5
optimizer_lr_backbone: 1e-5
optimizer_weight_decay: 1e-4

steps: 11550000
batch_size: 8
num_workers: 4
prefetch_factor: 2
persistent_workers: true
log_freq: 1000
save_freq: 200000
env_eval_freq: 0
eval_steps: 0
seed: 1000
output_dir: outputs/train_act_50ep
use_policy_training_preset: true

wandb:
  enable: false
